git config --global user.name "SOME NAME"
git config --global user.email "SOME EMAIL"


------CREATING SSH TO COMMIT-----------

# in project folder 

mkdir ~/.ssh
cd ~/.ssh

command to generate ssh 

ssh-keygen.exe

-- Key will be generated

$ ls
id_rsa  id_rsa.pub
(venv)
